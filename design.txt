

Task:
We need to crawl the target URL (see TARGET_URL in .env) with authentication
(see AUTH_USERNAME / AUTH_PASSWORD in .env, not committed to git) to find out
all the password with format: VISUALPING{, sixteen hex characters, }.

- Every password is reachable from the homepage — a real browser can click its way to every page on the site. There is nothing to guess: no hidden URLs, no wordlists, no robots.txt tricks. But not everything a browser sees is an <a> tag in the HTML source.
- Passwords are not always in the visible text of an HTML page, and not always stored the way you'd first expect. Pages reference other kinds of resources too; some passwords live in those. Look closely at everything the server gives you.

Steps:
1 Try to walk through all the URLs
    1.1 set up maximum total as 500 to prevent infinit deep looking
        1.1.1 If we reach the 500, we need to log out our queue since I need to find out how many unvisited url we have so we can enhance the config of maximum
    1.2 use bfs to find out, store the queue and visisted
    1.3 for url need param like page, we need to try the param with only 2 trivails, example: page1 page2
    1.4 all requests need to use our authen apply above (loaded from .env, see AUTH_USERNAME / AUTH_PASSWORD)
    1.5 ways to find out URLs
        1.5.1 <a>
        1.5.2 #JOBS: list all places we can find out the URLs, like form, click event...(need to set up title, where, how)

2 vectors to extract the password
# JOBS: list all places the password might can be extracted. (need to set up title, where, and its format, and how to find out the password)
2.1 http response
    2.1.1 headers
    2.1.2 body
2.2 HTML
2.3 css
2.4 js

3 audit log
3.1 includes: timestamp, resource, which vectors, url path, status, password if found

4 coding restriction
4.1 python
4.2 dependencies:
    4.2.1 playwright
    4.2.2 python-dotenv (loads credentials from .env, keeps them out of git)
    